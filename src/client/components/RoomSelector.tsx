import React from 'react';

const RoomSelector = () => {

  // depending on if the current room is null
  // render either a join room form
  // or a leave room button

  return (
    <div>
      <p>Insert a form with a button</p>
    </div>
  )

};

export default RoomSelector;